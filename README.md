# SnitchBot
Snitchbot analyzes GitHub repositories to estimate if code is AI-generated or human-written. It uses commit patterns, code structure, and heuristic scoring to flag suspicious repos and provide a clear AI likelihood score with simple explanations.
